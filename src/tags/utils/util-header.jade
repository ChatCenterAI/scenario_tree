util-header
   

  style(scoped, type='less').
    :scope {
      position: fixed;
      z-index: 101;
      top: 0;
      left: 0;
      display: block;
      width: 100%;
      height: 48px;
      background: #1976d2;
      filter: drop-shadow(1px 1px 1px rgba(0,0,0,0.4));
    }

  script.

    var self = this;

    this.on('mount', function(){

    });